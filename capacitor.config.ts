import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.erinckaratas.firebaseCapPush',
  appName: 'firebaseCapPush',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
